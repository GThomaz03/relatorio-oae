# Ambiente desktop (Electron + backend empacotado)
OAE_PORT=8765
LOG_LEVEL=INFO

# Opcional em dev — padrão: %LOCALAPPDATA%/OAE Report Generator/data
# OAE_DATA_DIR=C:\Users\SEU_USUARIO\AppData\Local\OAE Report Generator\dev-data

# Ativa CORS ampliado mesmo sem OAE_DATA_DIR (dev Electron)
# OAE_DESKTOP=1

# Somente produção PyInstaller — definido automaticamente pelo Electron
# OAE_BUNDLE_DIR=
